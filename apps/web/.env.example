# Frontend environment (copy to apps/web/.env.local for local dev)
AGENT_API_URL=http://localhost:8000
# Optional — must match agent AGENT_API_KEY when auth is enabled (server-side only)
AGENT_API_KEY=
